<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sime.itanortegaortega.astross">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignoActivity" />
        <activity android:name=".DiarioActivity" />
        <activity android:name=".SemanalActivity" />
        <activity android:name=".CompatibilidadActivity" />
        <activity android:name=".NumerosActivity" />
        <activity android:name=".FondosActivity" />
        <activity android:name=".PnlActivity" />
        <activity android:name=".RecomendarActivity" />
        <activity android:name=".AboutActivity"></activity>
    </application>

</manifest>